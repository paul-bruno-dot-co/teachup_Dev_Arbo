<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Centered Image Landing Page</title>
    <style>
        /* Reset body margin and padding */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f0f0f0; /* Light gray background */
        }

        /* Centering and responsive styling */
        .image-container {
            display: flex;
            justify-content: center;
            align-items: center;
            border: 10px solid black; /* Black border around the image */
            padding: 5px;
            max-width: 90%; /* Limits the width to 90% of the viewport */
            max-height: 90%; /* Limits the height to 90% of the viewport */
            box-sizing: border-box;
        }

        /* Image styling */
        .image-container img {
            max-width: 100%;
            max-height: 100%;
            display: block;
        }
    </style>
</head>
<body>

<div class="image-container">
    <img src="teachup_arbo.jpeg" alt="Centered Image">
</div>

</body>
</html>
